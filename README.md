# Truffle_configurations

Firstly install @truffle/hdwallet-provider: First, install the package using npm or yarn:

### Using npm:

`npm install @truffle/hdwallet-provider`
### Using yarn:

`yarn add @truffle/hdwallet-provider`
